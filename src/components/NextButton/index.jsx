import React from "react";
import s from "./s.module.css";

const NextButton = () => {
  return (
    <button className={s.container} type="submit">
      Next –{">"}
    </button>
  );
};

export default NextButton;
